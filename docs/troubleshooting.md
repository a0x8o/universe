## Can't see reports because of WebSockets `404` errors in browser console

If you are running Netsil AOC behind a load balancer, you need to allow `WebSockets` forwarding via load balancer `Layer-4` configuration.

